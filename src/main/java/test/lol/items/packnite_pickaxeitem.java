package test.lol.items;

import net.fabricmc.fabric.api.client.networking.v1.C2SPlayChannelEvents.Register;
import net.minecraft.item.PickaxeItem;

public class packnite_pickaxe extends PickaxeItem {
    public packnite_pickaxe(ToolMaterial material, int attackDamage, float attackSpeed, Settings settings) {
        super(material, attackDamage, attackSpeed, settings);

        
    }
}